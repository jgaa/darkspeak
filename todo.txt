Development todo's

 - Implement protocol layer
    ~ Make an Engine object to keep track of the Peers
        - Add counters for incoming connections and requests

    ~ Make a Peer object
        - Keeps track of connections, timeouts, state

 - TC protocol:
    - Implement generic send request method
        v Encoding
        v Decoding
        - Sanity checks
    - Generate cryptographically safe cookie

 - Implement the Api for local use (no proxy)
    - Implement event notifications from the TC layer
    - Implement the full connect work-flow:
        [pong/add_me] --> notification +
                                       |
                      <-- allow_buddy--+
    - Queue incoming messages before the peer is approved
    - Queue outgoing messages when they can't be sent.
    - Implement file transfer
    - Implement avatar


 - Implement a simple GUI using QT QML.
